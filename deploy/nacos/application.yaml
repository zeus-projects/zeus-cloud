spring:
  main:
    allow-bean-definition-overriding: true
  redis:
    host: ${REDIS_HOST:zeus-redis}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:1qaz@wsx?S}
  data:
    redis:
      host: ${REDIS_HOST:zeus-redis}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:1qaz@wsx?S}

springdoc:
  api-docs:
    enabled: true
    path: /springdoc
  swagger-ui:
    enabled: false
