server:
  port: 4000

# 数据库配置
spring:
  #  profiles:
  #    active: dev
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher # 解决 SpringFox 与 SpringBoot 版本不兼容的问题
  application:
    name: zeus-upms
  cloud:
    nacos:
      discovery: # 使用注册中心
        server-addr: ${NACOS_HOST:localhost}:${NACOS_PORT:8848} # Nacos服务注册中心地址
      config: # 使用配置中心
        server-addr: ${spring.cloud.nacos.discovery.server-addr} # Nacos作为配置中心地址
        prefix: ${spring.application.name}
        file-extension: yaml # 指定yaml格式的配置
#        namespace: ${spring.profiles.active} # 命名空间，默认为 public
#        group: DEFAULT_GROUP # 分组，默认为 DEFAULT_GROUP

#spring:
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    username: root
#    password: root
#    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB:zeus}?characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=GMT%2B8&nullCatalogMeansCurrent=true&allowPublicKeyRetrieval=true
